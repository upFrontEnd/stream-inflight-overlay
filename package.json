{
  "name": "stream-inflight-overlay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --open",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview --open"
  },
  "devDependencies": {
    "sass": "^1.104.1",
    "vite": "^8.3.0"
  }
}
